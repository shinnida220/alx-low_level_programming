#include <stdio.h>

/**
 * main - a program that prints its name
 * @argc: Argument count
 * @argv: Array of arguments
 * Return: int 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
